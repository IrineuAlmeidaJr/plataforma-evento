# Quando utilizamos aqui nossas query tem que ter um nome
query GetLessons {
    lessons(orderBy: availableAt_ASC, stage: PUBLISHED) {
        id
        lessonType
        availableAt
        title
        slug
    }
}